When I invoke this command, follow this structured analysis process for the task at hand. Do NOT skip steps. Do NOT answer until you have completed all evidence-gathering steps.

## Step 1: Parse the Request

Restate exactly what I am asking in your own words. Break it into numbered sub-questions if there are multiple parts. List them explicitly.

For each sub-question, classify it:
- FACTUAL — answerable by reading code or running a command
- ANALYTICAL — requires reasoning across multiple pieces of evidence
- UNCERTAIN — you may not be able to fully answer without more information

You MUST NOT proceed until you have this list.

## Step 2: Plan the Investigation

For EACH sub-question, list what you need to examine:

- Sub-question 1 → need to read: [specific files, functions, docs]
- Sub-question 2 → need to read: [specific files, functions, docs]

Announce your plan: "I need to check [X, Y, Z] before I can answer this."

Identify which sub-questions are INDEPENDENT of each other. For independent questions, you MUST use the Task tool to investigate them in parallel with subagents. Each subagent MUST:
- Have ONE clear question to answer
- Read the actual code to answer it
- Report findings with specific file names, line numbers, and code citations
- Flag anything it could not verify

For dependent questions (where answer B requires answer A), investigate sequentially.

## Step 3: Gather Evidence

For EACH item identified in Step 2:
- You MUST open and read the actual file or run the actual command
- You MUST quote or cite the specific relevant code with file paths and line numbers
- If you cannot access something, say so explicitly — do NOT guess what it contains
- If a file is large, focus on the relevant sections but note what else exists

Evidence means code you read, output you saw, files you opened. Nothing else counts.

## Step 4: Answer Each Sub-Question

For each sub-question, you MUST:
- State your answer in clear terms
- Cite the specific evidence that supports it (file:line, command output, etc.)
- Distinguish between KNOW (read the code) and INFER (logical conclusion from evidence)
- If you are uncertain, state what you ARE certain about and what remains uncertain

You MUST NOT present any answer without a supporting citation. An answer without evidence is a guess.

## Step 5: Cross-Verify

You MUST NOT present your final response until every item below passes:
- Does any claim lack a specific code citation? → Go get the evidence now.
- Did you use "probably", "should", or "likely" anywhere? → Replace with certainty or explicit uncertainty.
- Are there contradictions between your sub-answers? → Resolve with evidence.
- Did you assume anything you could have checked? → Go check it.
- Would any of your claims be wrong if you re-read the code right now? → Re-read it.

Skipping this step has never produced a correct answer. Do not skip it.

## Step 6: Final Response

You MUST present your complete answer with:
- Clear conclusions tied to specific evidence (file paths, line numbers, command outputs)
- Explicit uncertainty flags for anything you could not verify: "I have not verified X because Y"
- No hedging words unless explicitly flagged as uncertainty
- Actionable next steps if applicable

If at ANY point during this process you realize you don't know something: say "I don't know this yet, let me check" and investigate. Do not fill the gap with a guess. Guessing produces wrong answers. Without exception.
